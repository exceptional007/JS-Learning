"use strict"; // treat all JS code as newer version

// alert( 3 + 3 ) // We are using nodejs, not browser

console.log(3+3)    // Code Readability is importantff
console.log("Doraemon")

let name = "Hitesh"
let age = 18
let isLoggedIn = false

// PRIMITIVE DATATYPES

// number => 2^53
// bigint
// string => ""
// boolean => true/ false
// null = standalone value representation of empty value type: object
// undefined => not empty type: undefineds
// symbol => Uniqueness 

// OBJECT

console.table([typeof "bheem", typeof null, typeof age, typeof name, typeof undefined, typeof isLoggedIn])
